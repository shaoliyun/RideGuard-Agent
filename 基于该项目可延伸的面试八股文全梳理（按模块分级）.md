# 基于该项目可延伸的面试八股文全梳理（按模块分级）

该项目覆盖了**AI Agent 后端、RAG 检索、缓存架构、响应式编程、分布式存储**五大当前后端面试最高频赛道，以下是按技术栈和项目设计点拆解的所有可延伸八股，标注了**面试出现频率**和**项目关联度**。

## 一、Spring AI & 多 Agent 核心八股（★★★★★ 必问，项目核心）

### 基础题

1. Spring AI 的核心组件有哪些？ChatClient、Prompt、Message、FunctionCallback 分别是什么作用？
2. 什么是大模型的工具调用（Function Calling）？它的基本执行流程是什么？
3. 多 Agent 架构和单 Agent 架构的区别是什么？各自的适用场景？
4. 什么是意图路由？在 Agent 系统中为什么需要做意图路由？
5. 什么是槽位填充（Slot Filling）？它在任务型 Agent 中的作用是什么？
6. 什么是 HITL（人机在环）？为什么在涉及交易的 Agent 系统中必须引入 HITL？
7. Spring AI Alibaba Agent 和原生 Spring AI 的区别是什么？Alibaba Agent 提供了哪些增强能力？

### 进阶题

1. Spring AI 中 Function Calling 的实现原理是什么？它是怎么把 Java 方法自动转换成大模型可识别的工具定义的？
2. 工具调用的幂等性怎么保证？如果大模型重复调用同一个下单工具怎么办？
3. 工具调用的超时、异常、重试机制怎么设计？如果某个工具调用失败了，Agent 应该怎么处理？
4. 多 Agent 之间的通信方式有哪些？你这个项目中 OrderAgent 和 SupportAgent 是怎么交互的？
5. 意图路由的两种实现方式（规则引擎路由 vs 大模型路由）的优缺点对比？你是怎么选择的？
6. Agent 的状态管理怎么做？怎么在多轮对话中保存和恢复 Agent 的执行状态？
7. 怎么限制大模型的工具调用权限？防止模型调用未授权的业务接口（比如直接删除订单）？

### 项目深挖题

1. 你这个项目中，订单 Agent 的槽位填充和 HITL 确认机制具体是怎么实现的？为什么要把中间状态存在 Redis 而不是 MySQL？
2. 如果用户在确认订单的过程中修改了起点或终点，你的系统是怎么处理状态更新的？
3. 你设计的 FallbackAgent 的触发条件是什么？它和 SupportAgent 的职责边界怎么划分？
4. 怎么防止大模型 "幻觉" 导致的错误工具调用？你在项目中做了哪些校验措施？
5. 如果同时有 1000 个用户在和 Agent 对话，你的系统怎么保证每个 Agent 的状态互不干扰？

## 二、Redis 深度八股（★★★★★ 必问，项目核心缓存）

### 基础题

1. Redis 为什么快？它的单线程模型有什么优缺点？
2. Redis 的过期策略有哪些？惰性删除、定期删除、内存淘汰机制的区别？
3. Redis 的持久化方式 RDB 和 AOF 的区别？各自的优缺点和适用场景？
4. 什么是缓存穿透、缓存击穿、缓存雪崩？分别怎么解决？
5. Redis 支持哪些数据类型？你这个项目中用了哪些数据类型？分别存什么数据？
6. Redis 的事务和 MySQL 的事务有什么区别？Redis 事务能保证原子性吗？

### 进阶题

1. Redis 的内存模型是什么？String 类型的底层实现是怎样的？
2. Redis 的分布式锁怎么实现？Redisson 的可重入锁原理是什么？
3. Redis 和 MySQL 的数据一致性怎么保证？先更数据库还是先删缓存？为什么？
4. Redis 的集群模式有哪些？主从、哨兵、Cluster 的区别和适用场景？
5. Redis 的 Pipeline 和事务的区别？什么时候用 Pipeline？
6. Redis 的大 Key 问题怎么发现和解决？你这个项目中有没有可能出现大 Key？

### 项目深挖题

1. 你设计的 "Heap+Redis+MySQL" 多层记忆体系，各层的职责边界是什么？为什么不全部存在 Redis？
2. 对话上下文的过期时间怎么设置？如果用户隔了一天回来继续对话，你的系统怎么恢复上下文？
3. 你提到的 "工具结果指针化存储" 具体是怎么实现的？为什么能减少 Token 消耗？
4. 如果 Redis 挂了，你的对话系统会出现什么问题？你做了哪些降级措施？
5. 高并发下，Redis 的热点 Key 问题怎么解决？比如某个热门地点的 POI 信息被频繁查询。

## 三、Elasticsearch & RAG 八股（★★★★ 高频，AI 后端核心）

### 基础题

1. 什么是 RAG（检索增强生成）？它解决了大模型的什么问题？
2. RAG 的基本流程是什么？分为哪几个核心阶段？
3. 向量检索和全文检索（BM25）的区别是什么？各自的优缺点？
4. Elasticsearch 的核心概念：索引、文档、分片、副本分别是什么？
5. ES 的倒排索引原理是什么？和正排索引的区别？
6. 什么是向量数据库？ES 的向量存储能力和专用向量数据库（如 Milvus）的区别？

### 进阶题

1. 为什么 RAG 需要做 "混合检索"（向量 + 全文）？只做向量检索有什么问题？
2. 什么是 RRF（倒数秩融合）排序？它的原理是什么？为什么比简单的加权融合效果好？
3. RAG 的召回率和准确率怎么优化？你在项目中做了哪些优化措施？
4. ES 的分片数怎么设置？分片过多或过少会有什么问题？
5. ES 的写入性能怎么优化？批量写入、刷新间隔、副本数怎么调整？
6. RAG 中的文档分块策略怎么设计？分块大小和重叠大小怎么选择？

### 项目深挖题

1. 你这个项目中，为什么用 ES 做 RAG 而不是专用向量数据库？
2. 你提到的 "按 groupId 去重" 具体是怎么实现的？为什么要做这一步？
3. 如果知识库更新了，你的 RAG 系统怎么实时更新向量索引？
4. 对于用户的模糊问题，你的 RAG 系统怎么提高召回率？
5. 怎么评估你的 RAG 系统的效果？你用了哪些评估指标？

## 四、MySQL & MongoDB 八股（★★★★ 高频）

### MySQL 部分

1. MySQL 的事务隔离级别有哪些？各自解决了什么问题？默认隔离级别是什么？
2. MySQL 的索引类型有哪些？聚簇索引和非聚簇索引的区别？
3. 什么是覆盖索引？什么是回表？怎么避免回表？
4. MySQL 的慢查询怎么优化？你有哪些排查步骤？
5. MySQL 的分库分表怎么做？水平分表和垂直分表的区别？
6. MySQL 的 MVCC 原理是什么？它是怎么实现事务隔离的？

### MongoDB 部分

1. MongoDB 和 MySQL 的区别是什么？各自的适用场景？
2. MongoDB 的文档模型有什么优势？为什么适合存储非结构化数据？
3. MongoDB 的索引类型有哪些？和 MySQL 的索引有什么区别？
4. MongoDB 的分片集群原理是什么？分片键怎么选择？
5. MongoDB 的事务支持怎么样？和 MySQL 的事务有什么区别？

### 项目深挖题

1. 你这个项目中，为什么用 MySQL 做对话上下文的长期持久化，而不是 MongoDB？
2. 对话上下文表的结构是怎么设计的？你建了哪些索引？为什么？
3. MongoDB 在你的项目中用来存什么数据？为什么选择 MongoDB 而不是其他数据库？
4. 如果 MySQL 的单表数据量过大（比如超过 1000 万条对话记录），你会怎么优化？

## 五、Reactor Flux & 响应式编程八股（★★★ 中高频）

1. 什么是响应式编程？它和传统的命令式编程有什么区别？
2. Flux 和 Mono 的区别是什么？各自的适用场景？
3. 什么是背压（Backpressure）？Reactor 是怎么实现背压的？
4. Reactor 的线程模型是什么？Scheduler 有哪些类型？怎么选择？
5. 响应式编程的错误处理方式有哪些？onErrorReturn、onErrorResume、onErrorMap 的区别？
6. Spring WebFlux 和 Spring MVC 的区别？各自的优缺点和适用场景？
7. 为什么在 AI Agent 系统中适合使用响应式编程？你这个项目中用 Reactor Flux 解决了什么问题？

## 六、系统设计 & 架构八股（★★★★ 高频，项目亮点）

1. 高并发下的 Agent 系统怎么设计？限流、降级、熔断怎么做？
2. 分布式系统下的一致性问题怎么解决？你这个项目中哪些地方需要保证一致性？
3. 怎么设计一个可扩展的多 Agent 系统？如果要新增一个 Agent（比如支付 Agent），怎么最小化改动？
4. 业务风险控制怎么设计？除了 HITL，你还能想到哪些防止模型误操作的措施？
5. 怎么监控一个 AI Agent 系统？需要监控哪些指标？（比如工具调用成功率、平均响应时间、Token 消耗）
6. 如果你的系统要支持 10 万并发用户，你会怎么做架构升级？

## 七、Java & Spring Boot 基础八股（★★★★ 必问，打底）

1. Java 的内存模型（JMM）是什么？volatile 关键字的作用？
2. Java 的线程池参数有哪些？怎么合理设置线程池大小？
3. Java 的并发集合有哪些？ConcurrentHashMap 的原理是什么？
4. Spring Boot 的自动配置原理是什么？@SpringBootApplication 注解包含哪些注解？
5. Spring 的 IOC 和 AOP 原理是什么？AOP 的应用场景有哪些？
6. Spring 的事务管理机制是什么？@Transactional 注解的失效场景有哪些？
---

# 参考回答版：结合 TaxiAgent 项目口径

下面内容按“项目怎么做 + 通用原理 + 可优化点”的方式整理，适合面试时直接展开回答。

## 一、Spring AI & 多 Agent

### 1. Spring AI 的核心组件有哪些？

Spring AI 的核心组件包括 `ChatClient`、`ChatModel`、`Prompt`、`Message`、`ToolCallback` 等。

在本项目中：

- `ChatServiceImpl` 使用 `ChatClient` 做前置意图分类。
- `OrderAgent`、`DailyAgent`、`SupportAgent` 使用 `ChatModel` 进行流式对话和工具调用。
- `Prompt` 用来组合系统提示词、历史消息和用户输入。
- `Message` 表示系统消息、用户消息、助手消息和工具响应消息。
- `ToolCallback` 把 Java 后端能力暴露给大模型调用。

可以这样总结：`ChatClient/ChatModel` 负责调用模型，`Prompt/Message` 负责组织上下文，`ToolCallback` 负责把模型决策连接到真实业务方法。

### 2. 什么是工具调用 Function Calling？

工具调用就是让模型在需要真实业务能力时，不直接编造答案，而是返回一个工具调用请求，由后端执行真实方法。

基本流程是：

1. 后端用 `@Tool` 或 `ToolCallbacks.from(...)` 注册工具。
2. 模型根据 prompt 和工具描述决定调用哪个工具。
3. 模型返回 `toolCall`，包括工具名和参数。
4. 后端解析参数，执行 Java 方法。
5. 后端把结果封装成 `ToolResponseMessage` 放回上下文。
6. 模型基于工具结果继续推理或生成最终回答。

本项目中 `OrderAgent` 会调用 `saveNewOrderParam`、`isNewOrderReady`、`getEstRouteAndPrice`、`createOrder` 等工具；`SupportAgent` 会调用 `searchOrderById`、`searchOrdersByCount`、`searchKnowledgeBase` 等工具。

### 3. 多 Agent 和单 Agent 的区别是什么？

单 Agent 是一个大 prompt 加一组大工具，所有任务都由一个智能体处理。优点是结构简单，缺点是 prompt 长、工具多、容易误调用。

多 Agent 是按业务领域拆分多个智能体。项目里拆成：

- `OrderAgent`：新订单下单全流程。
- `DailyAgent`：天气、地点、估价、常用地点。
- `SupportAgent`：查单、取消、投诉、工单、RAG。
- `FallbackAgent`：兜底和非业务问题。

多 Agent 的优点是职责清晰、prompt 更短、工具集合更小、可维护性更好。缺点是需要先做好意图路由，跨 Agent 的多意图任务也更难处理。

### 4. 什么是意图路由？

意图路由就是先判断用户当前请求属于哪个业务领域，再交给对应 Agent。

本项目的路由流程是：

```text
ChatController
-> ChatServiceImpl
-> 分类模型输出 ORDER/DAILY/SUPPORT/OTHER/DANGER
-> switch 分发到对应 Agent
```

分类器当前只输出单个标签。所以一句话包含多个意图时，系统会选一个主意图，不会真正拆成多个子任务队列。

### 5. 什么是槽位填充？

槽位填充就是把自然语言中的业务参数抽取成结构化字段。

在下单场景中，用户说：

```text
帮我从杭州东站打车去西湖，快车，现在出发
```

系统需要抽取：

- 起点地址和经纬度
- 终点地址和经纬度
- 车型
- 是否预约
- 是否加急

本项目中 `OrderAgent` 调用 `saveNewOrderParam()` 把这些字段写入 Redis，再调用 `isNewOrderReady()` 检查是否齐全。

### 6. 什么是 HITL？为什么下单必须引入？

HITL 是 Human In The Loop，也就是人机在环。对于交易、支付、下单、取消订单等高风险动作，不能让模型直接执行，必须让用户确认。

本项目的做法是：

1. `OrderAgent` 补齐订单槽位。
2. 调用 `getEstRouteAndPrice()` 估算路线和价格。
3. 调用 `notifyUser()` 触发 `confirm` 事件。
4. Redis 中写入 `break=yes`，暂停流程。
5. 用户确认后调用 `/chat/v2/r/{id}`。
6. `resume()` 恢复流程，调用 `markOrderReadyForCreate()` 和 `createOrder()`。

这能防止模型误下单，也方便恢复和审计。

### 7. 工具调用如何保证幂等性？

不能依赖模型保证幂等，必须由后端状态机控制。

本项目中的例子：

- 创建订单前必须存在 `ReadyForCreate`。
- 取消订单前必须先调用 `verifyCancelConditions()`，写入 `readyForCancel`。
- 用户确认订单时通过 `break=yes` 控制恢复流程。

生产环境可以进一步加入：

- 幂等 token
- 业务唯一键
- 状态机校验
- 乐观锁
- 最大工具调用轮数
- 重复参数检测

### 8. Agent 状态如何保存和恢复？

本项目主要靠三类状态：

- 对话消息：保存在 `MessageMemory` 中，支持 Heap、Redis、MySQL 多层存储。
- 订单草稿：保存在 Redis 的 `chat:info:{chatId}`。
- 确认断点：通过 Redis 的 `break=yes` 标记。

当用户确认后，`OrderAgent.resume()` 会检查 `break`，找到历史中的 `notifyUser` 工具调用，再把用户确认结果作为 `ToolResponseMessage` 放回上下文继续执行。

## 二、Redis

### 1. Redis 为什么快？

Redis 快的原因主要有：

- 数据存储在内存中。
- 使用高效的数据结构。
- 基于 I/O 多路复用处理网络请求。
- 命令执行主要是单线程，减少锁竞争。
- 协议简单，处理开销小。

单线程的优点是实现简单、避免并发锁问题；缺点是如果某个命令执行很慢，会阻塞后续请求，所以要避免大 key、慢查询和复杂 Lua。

### 2. Redis 在项目里存什么？

项目中 Redis 主要存短期状态：

- 对话分类结果 `classification`
- 订单草稿槽位
- 确认断点 `break=yes`
- 取消订单前置状态 `readyForCancel`
- 工具结果缓存
- 短期对话上下文缓存

这些数据读写频繁、生命周期短、需要快速恢复，很适合 Redis。

### 3. 为什么是 Heap + Redis + MySQL 多层记忆？

三层职责不同：

- Heap：本机内存，速度最快，适合热点对话。
- Redis：跨请求、跨实例共享的短期缓存。
- MySQL：长期持久化和审计。

不全部放 Redis，是因为 Redis 成本高，长期历史数据量大，而且 MySQL 更适合做持久化、查询和数据审计。

### 4. 缓存穿透、击穿、雪崩是什么？

缓存穿透：查询不存在的数据，请求一直打到数据库。解决方式：空值缓存、布隆过滤器、参数校验。

缓存击穿：热点 key 过期，大量请求同时打到数据库。解决方式：互斥锁、逻辑过期、热点 key 不过期。

缓存雪崩：大量 key 同时失效或 Redis 故障。解决方式：随机 TTL、多级缓存、限流、降级。

### 5. Redis 和 MySQL 一致性怎么保证？

常见做法是先更新数据库，再删除缓存。因为数据库是事实源，缓存删除后，下次查询会回源加载新数据。

如果一致性要求更高，可以使用：

- 延迟双删
- MQ 异步删除
- binlog 监听
- 分布式锁
- 版本号校验

本项目中 Redis 更多是流程状态和草稿数据，不完全等同于 MySQL 事实数据，因此关键业务结果仍以 MySQL 为准。

### 6. 项目里可能出现哪些大 key？

可能出现大 key 的地方：

- 长对话历史
- 大型工具调用结果
- 热门 POI 查询缓存
- 订单草稿里塞入过多字段

项目用工具结果指针化缓解这个问题：长工具结果不直接塞进模型上下文，而是记录 `call_id`，需要时再通过 `getCalling(call_id)` 反查。

## 三、Elasticsearch & RAG

### 1. 什么是 RAG？

RAG 是 Retrieval-Augmented Generation，检索增强生成。它通过外部知识库检索相关内容，再让大模型基于检索结果回答。

它主要解决：

- 模型知识过期
- 模型幻觉
- 业务规则不可控
- 私有知识无法直接被模型知道

本项目中 `SupportAgent` 对规则、售后、费用说明等问题，会调用 `RagTool.searchKnowledgeBase()`。

### 2. RAG 基本流程是什么？

本项目的 RAG 流程是：

1. 用户问题进入 `SupportAgent`。
2. Agent 判断需要查知识库。
3. 调用 `searchKnowledgeBase(query)`。
4. `RagService` 生成 query embedding。
5. ES 同时做向量检索和 BM25 检索。
6. 用 RRF 融合排序。
7. 按 `groupId` 去重。
8. 返回 Top 5 知识给 Agent。
9. Agent 基于检索结果回答用户。

### 3. 向量检索和 BM25 有什么区别？

向量检索关注语义相似，适合同义表达、模糊问题。例如“车费不对”和“价格异常”可能语义接近。

BM25 关注关键词匹配，适合订单号、规则名、专有名词等精确文本。

只做向量检索可能漏掉关键词强相关内容，只做 BM25 又不理解语义。本项目用混合检索提高召回率。

### 4. 什么是 RRF？

RRF 是 Reciprocal Rank Fusion，倒数秩融合。它不直接比较不同检索方式的原始分数，而是根据排名计算融合分：

```text
score = 1 / (k + rank)
```

如果一个文档在向量检索和 BM25 中排名都靠前，它的融合分会更高。

RRF 的优点是不用手动归一化不同检索器的分数，比简单加权更稳定。

### 5. ES 分片怎么设置？项目设置了吗？

ES 的分片包括主分片和副本分片。主分片决定数据切分和写入分布，副本分片提供高可用和读扩展。

分片太多会增加内存和查询协调成本；分片太少会影响扩展性和并行能力。

本项目目前没有显式设置 `number_of_shards` 和 `number_of_replicas`。`chat-es-setting.json` 只配置了分词器和 mapping，`QaDocument` 也只是声明了：

```java
@Document(indexName = "qa_knowledge_base")
```

如果索引自动创建，就走 Elasticsearch 默认配置。

### 6. 为什么项目用 ES 做 RAG，而不是 Milvus？

因为项目同时需要：

- BM25 全文检索
- 向量检索
- 字段过滤
- 文档更新
- Java/Spring 集成

ES 一套系统就能完成这些能力，工程复杂度低。Milvus 更适合超大规模纯向量检索，但如果还要做全文检索，通常还要再接 ES。

### 7. groupId 去重怎么做？

项目里一个答案可能对应多个相似问法，每个问法是 ES 中一个文档，但它们共享同一个 `groupId`。

检索后，Java 代码按融合分排序，然后遍历结果，用 `Set<String> seenGroupIds` 去重。这样避免同一个答案的多个问法重复占据 Top 结果。

### 8. 知识库更新后如何同步索引？

新增 QA 时：

- 先写 ES 文档。
- 再写 MySQL 的 `qa_info` 和 `qa_elastic_map`。
- 如果失败，补偿删除 ES 文档。

更新问题时：

- 重新生成 embedding。
- 更新 ES 中的 `question` 和 `questionVector`。
- 同步更新 MySQL 映射表。

删除时：

- 删除 ES 文档。
- 删除 MySQL mapping。
- 如果某个 group 已无问题映射，再删除答案记录。

## 四、MySQL & MongoDB

### 1. MySQL 在项目里负责什么？

MySQL 负责结构化核心业务数据，包括：

- 用户
- 订单
- 工单
- 对话消息
- 工具调用结果
- RAG 问答映射

它适合强一致、事务、索引查询和审计场景。

### 2. MongoDB 在项目里负责什么？

MongoDB 主要用于存储订单路线轨迹。

路线轨迹可能包含较长的 polyline、预估路线、真实路线等字段，结构相对灵活，不适合频繁关系查询，所以用 MongoDB 更自然。

### 3. MySQL 事务隔离级别有哪些？

四种隔离级别：

- 读未提交：可能脏读。
- 读已提交：解决脏读，但可能不可重复读。
- 可重复读：解决不可重复读，MySQL InnoDB 默认级别。
- 串行化：最强隔离，但并发性能最低。

InnoDB 通过 MVCC、undo log、read view 实现快照读。

### 4. 慢查询怎么优化？

排查步骤：

1. 查看慢查询日志。
2. 使用 `EXPLAIN` 看执行计划。
3. 检查是否走索引。
4. 检查扫描行数、回表次数、排序和临时表。
5. 优化 SQL 或加组合索引。
6. 必要时分页优化、冷热分离、分库分表。

对本项目，订单表常用索引应关注：

- `order_id`
- `user_id`
- `driver_id`
- `create_time`
- `order_status`

### 5. 为什么聊天上下文长期存 MySQL，而不是 MongoDB？

聊天消息是典型结构化数据，有明确字段：用户、会话、角色、内容、时间、顺序。它需要分页、排序、审计和恢复。

MySQL 更适合这类强结构化历史数据。MongoDB 更适合路线轨迹这类结构灵活、内容较长的文档。

### 6. 如果消息表超过千万怎么优化？

可以从几个方向优化：

- 按 `chat_id + id/create_time` 建组合索引。
- 只加载最近 N 条消息。
- 历史消息摘要化。
- 冷热数据归档。
- 按用户或时间分表。
- 工具大结果单独存储，避免撑大上下文。

## 五、Reactor Flux & 响应式编程

### 1. 什么是响应式编程？

响应式编程是面向异步数据流的编程模型。它不要求一次性返回完整结果，而是数据产生一段就推送一段。

AI Agent 很适合响应式，因为模型生成、工具调用、地图 API、RAG 检索都可能耗时。

### 2. Flux 和 Mono 的区别是什么？

`Mono` 表示 0 到 1 个元素，适合单次查询结果。

`Flux` 表示 0 到 N 个元素，适合流式响应。

本项目聊天接口返回：

```java
Flux<AgentEvent>
```

因为一次 Agent 执行可能产生多个事件：文本消息、工具开始、工具结果、确认事件、错误事件。

### 3. 什么是背压？

背压是消费者处理不过来时，对生产者进行限速或缓冲的机制。

项目中使用：

```java
Sinks.many().unicast().onBackpressureBuffer()
```

它把异步产生的 Agent 事件缓冲起来，再作为 Flux 返回给前端。

### 4. 为什么 AI Agent 适合用 Flux？

因为用户不应该长时间等待黑盒结果。

通过 Flux，前端可以逐步看到：

- 正在分类
- 正在查询订单
- 正在规划路线
- 正在计算价格
- 等待用户确认

这样体验更好，也更容易排查 Agent 执行过程。

### 5. 异常处理要注意什么？

流式接口内部异常不能只记录日志，否则前端会一直等待。

应该发送错误事件并结束流：

```java
sink.tryEmitNext(AgentEvent.error(...));
sink.tryEmitComplete();
```

或者使用 Reactor 的 `onErrorResume`、`onErrorReturn` 做兜底。

## 六、系统设计 & 架构

### 1. 高并发 Agent 系统怎么设计？

需要从几个层面做：

- 网关限流
- 用户级限流
- 模型调用并发控制
- 工具超时
- 熔断和降级
- Redis/MySQL/ES 连接池调优
- 异步线程池隔离
- 热点缓存
- 监控和告警

对 Agent 来说，还要限制最大工具调用轮数，避免模型陷入循环。

### 2. 分布式一致性问题怎么处理？

项目中需要关注一致性的地方：

- Redis 订单草稿和 MySQL 最终订单
- ES 知识索引和 MySQL QA 映射
- MongoDB 路线轨迹和 MySQL 订单 traceId
- 订单取消状态和取消费用

原则是 MySQL 作为核心事实源，Redis 是状态缓存，ES 是检索索引，MongoDB 是轨迹存储。跨存储失败时要有补偿和重建机制。

### 3. 如何扩展新的 Agent？

当前项目在 `ChatServiceImpl` 里用 `switch` 分发，新增 Agent 需要改分类 prompt、注入 Agent、改 switch。

更好的方式是 Agent Registry，记录：

- agentId
- 支持意图
- 工具集合
- prompt 版本
- 模型配置
- 是否启用
- 优先级

这样新增支付 Agent、司机 Agent 时，可以最小化代码改动。

### 4. 业务风控怎么设计？

除了 HITL，还可以做：

- 工具权限控制
- 参数来源校验
- 状态机校验
- 敏感操作二次确认
- 幂等 token
- 审计日志
- prompt injection 检测
- 最大工具轮数
- 异常降级到人工客服

本项目已有 `DANGER` 分类、下单确认断点、取消前置校验等基础能力。

### 5. Agent 系统要监控哪些指标？

核心指标包括：

- 意图分类准确率
- 每轮工具调用次数
- 工具调用成功率
- 工具调用耗时
- 模型响应耗时
- token 消耗
- RAG 命中率
- 订单创建成功率
- 用户确认率
- 异常率
- Redis/MySQL/ES 延迟

这些指标能帮助定位是模型问题、工具问题、外部 API 问题，还是数据层问题。

### 6. 支持十万并发怎么升级？

可以考虑：

- 服务水平扩容
- 网关限流和排队
- 模型调用异步化
- 工具线程池隔离
- Redis Cluster
- MySQL 分库分表和读写分离
- ES 集群扩容
- 热点数据缓存
- 消息队列削峰
- Agent 任务状态持久化
- 前端 SSE/WebSocket 长连接治理

## 七、Java & Spring Boot

### 1. JMM 是什么？volatile 有什么用？

JMM 是 Java 内存模型，规定线程之间如何通过主内存进行变量可见性同步。

`volatile` 的作用：

- 保证变量修改对其他线程可见。
- 禁止指令重排序。

但它不能保证复合操作的原子性，比如 `i++` 仍然不是线程安全的。

### 2. 线程池参数有哪些？

核心参数包括：

- corePoolSize
- maximumPoolSize
- keepAliveTime
- workQueue
- threadFactory
- rejectedExecutionHandler

AI Agent 中模型调用和外部 API 调用多为 I/O 密集型，可以适当提高线程数，但必须限制最大并发，避免压垮模型服务和第三方 API。

### 3. ConcurrentHashMap 原理是什么？

JDK 8 之后的 `ConcurrentHashMap` 主要通过 CAS、synchronized、数组 + 链表 + 红黑树实现并发安全。

读操作大多无锁，写操作只锁局部桶，降低锁粒度，提高并发性能。

### 4. Spring Boot 自动配置原理是什么？

`@SpringBootApplication` 包含：

- `@SpringBootConfiguration`
- `@EnableAutoConfiguration`
- `@ComponentScan`

自动配置通过条件注解判断 classpath、配置项、Bean 是否存在，然后决定是否创建对应 Bean。

### 5. IOC 和 AOP 是什么？

IOC 是控制反转，对象创建和依赖注入交给 Spring 容器管理。

AOP 是面向切面编程，用代理在方法前后增强逻辑。

本项目中权限校验 `@RequirePermission` 就是 AOP 的典型应用，通过切面统一检查用户角色。

### 6. @Transactional 什么时候会失效？

常见失效场景：

- 同类内部方法直接调用。
- 方法不是 `public`。
- 异常被捕获没有抛出。
- 默认只回滚 RuntimeException，抛 checked exception 未配置 rollback。
- 对象不是 Spring 容器管理的 Bean。
- 事务传播行为配置不当。

在本项目中，RAG 新增、删除、更新涉及 MySQL 和 ES，MySQL 可以靠事务回滚，ES 需要额外补偿。

## 面试总结话术

这个项目的核心不是简单让大模型聊天，而是把大模型放进一条可路由、可校验、可确认、可恢复、可追踪的业务链路里。

前置路由解决“交给哪个 Agent”，工具调用解决“怎么执行真实业务”，Redis 解决“多轮状态和断点恢复”，MySQL/MongoDB/ES 分别承担结构化业务、路线轨迹和知识检索，Flux 解决“执行过程可流式返回”。这套设计比普通聊天机器人更接近真实业务系统。
